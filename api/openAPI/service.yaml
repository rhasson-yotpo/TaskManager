openapi: 3.0.0
info:
  title: Tasks Service
  description: Corgis service
  version: 1.0

servers:
  - url: http://localhost:8080
    description: Local server


components:
  securitySchemes:
    bearer:
      type: http
      scheme: bearer

security:
  - bearer: []

paths:
  /tasks:
    $ref: 'corgisRequests.yaml#/tasks'